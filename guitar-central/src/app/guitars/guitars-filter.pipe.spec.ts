import { GuitarsFilterPipe } from './guitars-filter.pipe';

describe('GuitarsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GuitarsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
