import { CmsDropdownDirective } from './cms-dropdown.directive';

describe('CmsDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new CmsDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
